module Game.Colors exposing (navy)

{-| Shared color constants. The navy hex shared by buttons,
popup, board border, and section headings lives here so each
caller doesn't redeclare it.
-}


navy : String
navy =
    "#000080"
