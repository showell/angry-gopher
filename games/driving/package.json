{
  "name": "driving",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "description": "A tiny first-person driving game (browser canvas). TS source bundled to app.js by esbuild; see ./build.",
  "scripts": {
    "typecheck": "tsc --noEmit -p ."
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
