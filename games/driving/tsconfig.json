{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "allowImportingTsExtensions": true,
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noImplicitReturns": true,
    "noFallthroughCasesInSwitch": true,
    "verbatimModuleSyntax": true,
    "isolatedModules": true,
    "noEmit": true,
    "lib": ["ES2022", "DOM"],
    "skipLibCheck": true
  },
  "include": ["**/*.ts"],
  "comment-exclude": "mini_canvas.ts / snap_cat.ts are Node-side dev tools (Buffer/zlib/fs), not part of the browser bundle — kept out of the DOM-only typecheck so we needn't pull in @types/node. They are run directly via `node --experimental-strip-types`.",
  "exclude": ["node_modules", "mini_canvas.ts", "snap_cat.ts"]
}
