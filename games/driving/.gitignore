node_modules/
*.tsbuildinfo
# esbuild output (built by ./build, served locally)
/app.js

# cat snapshot tool output
/snap/

# native renderer build outputs (ops/build_safari_native, build.zig)
/zig-out/
/.zig-cache/
/native/safari_native
