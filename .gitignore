# Build outputs (binaries produced by go build in repo root)
/gopher-server
/health_check
/stress
/import
/crudgen
/db_query
/reorg

# Claude Code per-session state (scheduled_tasks.lock, settings.local.json)
/.claude/

# Editor temp files
*.swp

# Python bytecode caches
__pycache__/
*.pyc

# Stray compiled binary from `go build` with no -o (see cmd/ for proper output paths)
/angry-gopher

# LynRummy compiled bundles (generated by ops/build_elm and
# ops/build_engine_js — engine_glue.js is hand-written, kept).
/games/lynrummy/elm/puzzle.js
/games/lynrummy/elm/engine.js
