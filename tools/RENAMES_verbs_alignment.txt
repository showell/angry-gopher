# Align verbs.py names with their Elm siblings under
# Game.Agent.Verbs.elm. The Elm names use the `Prims` suffix
# to mark the per-verb-to-primitives translator helpers; the
# Python privates lacked that suffix.
#
# Also rename the public entry point: `step` → `move`. The
# unit-of-input is now a Move dataclass, not a generic "step",
# and Elm calls it `moveToPrimitives`.

step_to_primitives -> move_to_primitives

_extract_absorb -> _extract_absorb_prims
_free_pull -> _free_pull_prims
_push -> _push_prims
_splice -> _splice_prims
_shift -> _shift_prims
