/**
 * The template App smoke test does not apply — login talks to Keychain.
 * Unit tests live next to the modules under src/.
 */

test('placeholder so the default test file stays valid', () => {
  expect(true).toBe(true);
});
