#!/bin/bash
# CANONICAL DEV-LOOP SCRIPT — DO NOT REINVENT.
# Before writing a new ops/* script or composing an ad-hoc shell
# command for build / launch / test / regen, run `ops/list` to see
# what already exists.
#
# Lyn Rummy-subsystem gate (~30s warm): the common base (build + docs)
# plus the Elm client + TS solver/engine suites. Use this while working
# on Lyn Rummy — it SKIPS the chat client lint. For the slow tier (agent
# self-play + perf benches) use ops/check_full. Mirrors
# server/web <- server/lynrummy.

set -e

REPO_DIR="$(cd "$(dirname "$0")/.." && pwd)"
cd "$REPO_DIR"

t0_total=$SECONDS

./ops/check_common

echo
echo "===== ops/test_elm ====="
./ops/test_elm

echo
echo "===== ops/test_ts ====="
./ops/test_ts

echo
echo "Lyn Rummy gate passed in $((SECONDS - t0_total))s."
