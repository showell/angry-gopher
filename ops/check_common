#!/bin/bash
# CANONICAL DEV-LOOP SCRIPT — DO NOT REINVENT.
# Before writing a new ops/* script or composing an ad-hoc shell
# command for build / launch / test / regen, run `ops/list` to see
# what already exists.
#
# Shared base gate both subsystems need: the zig server compiles + its unit
# tests pass (ops/check_zig), the docs have no dead links, and every CSS class
# in any <style> block is still referenced. This is the "diamond floor" —
# composed by ops/check_chat, ops/check_lynrummy, and ops/check.

set -e

REPO_DIR="$(cd "$(dirname "$0")/.." && pwd)"
cd "$REPO_DIR"

echo "===== ops/check_zig ====="
./ops/check_zig

echo
echo "===== ops/test_docs ====="
./ops/test_docs

echo
echo "===== ops/test_css ====="
./ops/test_css
